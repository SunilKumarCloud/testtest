name: Daily 9 :30 PM IST run

on:
  schedule:
    - cron: "0 16 * * *"      # every day at 16:00 UTC == 21:30 IST
  workflow_dispatch:          # manual trigger for testing

jobs:
  daily-job:
    runs-on: ubuntu-latest

    steps:
      - name: Actual job logic
        run: |
          echo "🔥 running daily job at $(date -u) (≃ 9:30 PM IST)"
